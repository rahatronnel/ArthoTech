rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /employees/{userId} {
      // Allow any authenticated user to read their own employee document.
      // This is the minimum required rule for the login process to succeed.
      allow read: if request.auth != null && request.auth.uid == userId;
    }
  }
}
